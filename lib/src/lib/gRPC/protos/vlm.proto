syntax = "proto3";

package vlm;

message InferenceRequest {
  bytes image = 1;
  string config_json = 2;  // InferenceConfig
}

message Response {
  string text = 1;
}

service VLMServer {
  rpc Inference (InferenceRequest) returns (Response);
}
