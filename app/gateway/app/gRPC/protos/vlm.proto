syntax = "proto3";

message Image {
    bytes image = 1;
    string runner = 5;
}

message Response {
    string html = 1;
    string json = 2;
    string csv = 3;
    string text = 4;
    string markdown = 5;
}

service VLMServer {
    rpc GenerateResponse(Image) returns (Response);
}

