[project]
name = "gnosis-cli"
version = "0.1.0"
description = "CLI to access Gnosis cloud service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "pydantic>=2.12.5",
    "requests>=2.33.1",
]
